package parte1;

public class _01_TablaEmpleados {
    // Crea un programa en Java que se conecte a una base dee datos MySQL y cree una tabla llamada `empleados`. La tabla debe tener las siguientes columnas:
    /**
     * `id` (INT, AUTO-INCREMENT, PRIMARY KEY)
     * `nombre` (VARCHAR(50))
     * `puesto` (VARCHAR(50))
     * `salario` (DECIMAL(10, 2))
     */

}

